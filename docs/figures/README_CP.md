# CryptoPanic Ingestion — Note (Deprecated)

> **Status: UNUSED.** CryptoPanic's free tier serves an RSS feed that returns an HTML page, not valid RSS XML. The paid API tier requires a Pro subscription ($49/mo+). This pipeline is kept for reference but is not in active use.

The forward data pipeline currently uses 17 publisher RSS feeds instead (`snippets/crypto_rss_ingest.py`).
